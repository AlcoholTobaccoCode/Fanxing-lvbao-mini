<script lang="ts" setup>
import type { EasemobConversationItem } from "@/utils/easemob";
import ChatHistoryList from "./chat-history-list.uvue";

const emit = defineEmits<{
	(e: "select", item: EasemobConversationItem): void;
}>();

const handleSelect = (item: EasemobConversationItem) => {
	emit("select", item);
};
</script>

<template>
	<view class="easeim-history-root">
		<chat-history-list @select="handleSelect"></chat-history-list>
	</view>
</template>

<style lang="scss" scoped>
.easeim-history-root {
	@apply flex flex-col h-full;
}
</style>
